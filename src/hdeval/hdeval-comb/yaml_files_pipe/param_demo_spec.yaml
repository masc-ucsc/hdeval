description: Design a module that utilizes both a parameter and a localparam to set
  a 4-bit output 'o' to a specific value. The parameter should be the value 9, and
  the localparam should be value of the parameter plus one. 'o' will take the value
  of the localparam.
interface: module param_demo (output reg [3:0] o);
