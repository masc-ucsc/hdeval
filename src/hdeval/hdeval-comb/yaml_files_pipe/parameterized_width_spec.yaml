description: Design a module with a fixed width of 3. Initialize an output port named
  'out_o', whose length is 3 bits. The binary value held at 'out_o' will be all ones.
interface: module parameterized_width (output [2:0] out_o);
