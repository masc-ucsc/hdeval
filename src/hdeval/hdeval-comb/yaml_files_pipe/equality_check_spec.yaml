description: 'Design a module with inputs: [3:0]a, [3:0]b and output: [0:0]equal that
  checks if two 4-bit numbers are equal.'
interface: module equality_check (input [3:0] a, input [3:0] b, output equal);
