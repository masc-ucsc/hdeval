description: Create a module that demonstrates the difference between a `parameter`
  and a `localparam`. The parameters value should be 10 and the local parameters value
  should be one greater than that. Set 'o' to the value of the local parameter. The
  module has one output 4-bit `o`
interface: module param_difference_demo (output [3:0] o);
